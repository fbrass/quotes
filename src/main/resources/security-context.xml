<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:sec="http://www.springframework.org/schema/security"
        xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <!-- Uncomment to get debugging output from spring security -->
    <!--<sec:debug/>-->

    <sec:global-method-security jsr250-annotations="enabled" secured-annotations="enabled" />

    <!-- Exclude favicon.ico to avoid redirect after login -->
    <sec:http pattern="/favicon.ico" security="none"/>

    <sec:http xmlns="http://www.springframework.org/schema/security" auto-config="true" use-expressions="true">
        <intercept-url pattern="/s/*" access="permitAll"/>
        <intercept-url pattern="/s/**" access="permitAll"/>
        <intercept-url pattern="/s/pw*" access="permitAll"/>
        <intercept-url pattern="/s/pw/**" access="permitAll"/>
        <intercept-url pattern="/login*" access="permitAll"/>
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')"/>

        <form-login login-page="/login"
                    default-target-url="/home"
                    authentication-failure-url="/loginfailed"/>
        <logout logout-success-url="/" delete-cookies="true" invalidate-session="true"/>

        <remember-me data-source-ref="dataSource" user-service-ref="myUserDetailsService" key="tododont"/>

        <session-management invalid-session-url="/login">
            <concurrency-control max-sessions="1" expired-url="/"  error-if-maximum-exceeded="false" />
        </session-management>
    </sec:http>

    <bean id="myUserDetailsService" class="org.springframework.security.core.userdetails.jdbc.JdbcDaoImpl">
        <property name="dataSource" ref="dataSource"/>
        <property name="usersByUsernameQuery" value="select email,password,enabled from users where email = ?"/>
        <property name="authoritiesByUsernameQuery" value="select email,authority from authorities where email = ?"/>
    </bean>

    <bean id="passwordEncoder" class="org.springframework.security.crypto.password.StandardPasswordEncoder"/>

    <sec:authentication-manager xmlns="http://www.springframework.org/schema/security" alias="authenticationManager">
        <authentication-provider user-service-ref="myUserDetailsService">
            <password-encoder ref="passwordEncoder"/>
        </authentication-provider>
    </sec:authentication-manager>

</beans>
