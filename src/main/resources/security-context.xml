<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:beans="http://www.springframework.org/schema/beans"
        xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Uncomment to get debugging output from spring security -->
    <!--<debug/>-->

    <global-method-security jsr250-annotations="enabled" secured-annotations="enabled" />

    <!-- TODO session timeout configuration for automatic logout -->
    <!-- TODO logout page is missing -->
    <!-- TODO test persistent (remember me) logins -->
    <!-- TODO table persistent_logins is unused -->

    <http auto-config="true">
        <intercept-url pattern="/register*" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
        <intercept-url pattern="/register/**" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
        <intercept-url pattern="/login*" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
        <intercept-url pattern="/**" access="ROLE_USER"/>

        <form-login login-page="/login"
                    default-target-url="/home"
                    authentication-failure-url="/loginfailed"/>
        <logout logout-success-url="/" delete-cookies="true" invalidate-session="true"/>
        <remember-me user-service-ref="myUserDetailsService" key="tododont"/>
    </http>

    <beans:bean id="myUserDetailsService" class="org.springframework.security.core.userdetails.jdbc.JdbcDaoImpl">
        <beans:property name="dataSource" ref="dataSource"/>
        <beans:property name="usersByUsernameQuery" value="select email,password,enabled from users where email = ?"/>
        <beans:property name="authoritiesByUsernameQuery" value="select email,authority from authorities where email = ?"/>
    </beans:bean>

    <beans:bean id="passwordEncoder" class="org.springframework.security.crypto.password.StandardPasswordEncoder"/>

    <authentication-manager>
        <authentication-provider user-service-ref="myUserDetailsService">
            <password-encoder ref="passwordEncoder"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>
