<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <import resource="security-context.xml"/>

    <context:component-scan base-package="org.antbear.tododont.backend"/>

    <context:annotation-config/>

    <context:property-placeholder location="classpath:jdbc.properties"/>

    <bean id="dataSource"
          class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close"
          p:driverClass="${jdbc.driverClassName}"
          p:jdbcUrl="${jdbc.url}"
          p:user="${jdbc.username}"
          p:password="${jdbc.password}"/>

    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager"
          p:dataSource-ref="dataSource"/>

    <tx:annotation-driven transaction-manager="txManager"/>

    <!-- Bean to create the initial application setup, e.g. initial default users -->
    <bean class="org.antbear.tododont.util.InitialSetup" lazy-init="false" scope="singleton"
          p:dataSource-ref="dataSource"
          p:passwordEncoder-ref="passwordEncoder"/>

    <!-- Other beans -->

    <bean class="org.antbear.tododont.backend.service.userregistration.UserRegistrationMailSenderImpl"/>

    <!-- Mail -->

    <bean id="mailSender" class="org.antbear.tododont.util.MailSenderImpl"
          p:delegate-ref="mailSenderDelegate"/>

    <bean id="mailSenderDelegate" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="localhost"/>
    </bean>

    <bean id="mailTemplateMessage" class="org.springframework.mail.SimpleMailMessage">
        <property name="from" value="customerservice@tododont.tld"/>
        <property name="subject" value="Your login activation from tododont"/>
    </bean>

</beans>
